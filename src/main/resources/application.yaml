spring:
  application:
    name: spring-ai-agent-learning
  ai:
    openai:
      base-url: https://api.openai.com
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

server:
  port: 8080